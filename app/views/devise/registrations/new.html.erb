<h1 class="typography-title-b-lh150 text-dark-palette-p4 text-center mb-3">Registrar</h1>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: false }) do |f| %>
<%= render "devise/shared/error_messages", resource: resource %>
  <div class="grid gap-5">
    <%= f.fields_for :account, resource.build_account do |account_fields| %>
      <div class="grid gap-1">
        <%= account_fields.label :name, 'Empresa' , class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
        <%= account_fields.text_field :name, autofocus: true, autocomplete: "Empresa", class: "form-input", required: true %>
      </div>
      <div class="grid gap-1">
        <%= account_fields.label :site_url, 'Site da empresa' , class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
        <%= account_fields.text_field :site_url, autofocus: true, autocomplete: "Site da empresa", class: "form-input", required: true %>
      </div>
    <% end %>
    <div class="grid gap-1">
      <%= f.label :full_name, class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
      <%= f.text_field :full_name, autofocus: true, autocomplete: "full name", class: "form-input", required: true %>
    </div>
    <div class="grid gap-1">
      <%= f.label :phone, class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
      <%= f.text_field :phone, autofocus: true, autocomplete: "phone", class: "form-input", data: {controller: 'input-mask--input-phone'} %>
    </div>
    <div class="grid gap-1">
      <%= f.label :email, class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input", required: true %>
    </div>
    <div class="grid gap-1">
      <div class="flex items-center justify-between">
        <%= f.label :password, class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
        <%= link_to "Esqueceu a senha?", new_password_path(resource_name), class: "typography-text-m-lh150 text-dark-gray-palette-p1 hover:text-brand-palette-02 hidden" %>
      </div>
      <%= f.password_field :password, autocomplete: "new-password", class: "form-input w-full", required: true %>
      <% if @minimum_password_length %>
        <p class="invalid-feedback typography-sub-text-r-lh150 text-dark-gray-palette-p1">(<%= @minimum_password_length %> characters minimum)</p>
      <% end %>
    </div>
    <div class="grid gap-1">
      <%= f.label :password_confirmation, class: "typography-text-m-lh150 text-dark-gray-palette-p1" %>
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-input w-full", required: true %>
    </div>

    <%= f.submit "Cadastrar", class:"btn-primary cursor-pointer" %>
    <% end %>

  <%= render "devise/shared/links" %>
</div>
