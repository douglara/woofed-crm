<div class="col-12">
  <div>
    <header class="text-center m-3">
      <span class="badge badge-light">Planned</span>
    </header>
    <div class="activities">
      <% deal.flow_items.activities_not_done.each do | flow_item | %>
        <%= render "/deals/flow_items/item", flow_item: flow_item %>
      <% end %>
    </div>
  </div>

  <div>
    <header class="text-center m-3">
      <span class="badge badge-light">Done</span>
    </header>
    <div class="activities">
      <% deal.flow_items.wihtout_activities_not_done.each do | flow_item | %>
        <%= render "/deals/flow_items/item", flow_item: flow_item %>
      <% end %>
    </div>
  </div>
</div>