<nav class="flex flex-wrap z-10 items-center w-full justify-between py-4 px-8 bg-light-palette-p5 border-b-[1.5px] border-light-palette-p3 ">
  <div class="flex items-center">
    <%= link_to(:back, class: "flex border-solid border-2 border-light-palette-p3 rounded-md hover:bg-brand-palette-08 focus:bg-brand-palette-08 p-1.5") do %>
    <i data-lucide="chevron-left"></i>
    <% end %> 
  <div class="pl-4 lg:flex lg:items-center">
    <div class="h-full lg:border-r-2 border-light-palette-p3 lg:px-6 xl:px-16">
      <p class="typography-body-s-lh150 text-dark-gray-palette-p1"><%= @deal.name %></p>
    </div>
    <div class="flex items-center h-5">
      <div class=" flex items-center mr-[13px] lg:pl-6 xl:pl-16">
        <p class="m-0 p-0 typography-text-m-lh150 text-dark-gray-palette-p3"><%= @deal.stage.pipeline.name %></p>
      </div>
      <div class=" flex items-center mx-1.5 lg:mx-3 ">
        <i data-lucide="chevron-right" class="stroke-dark-gray-palette-p3"></i>
      </div>
      <div class=" flex items-center ml-1.5 lg:mx-3">
        <p class="m-0 typography-text-m-lh150 text-dark-gray-palette-p3"><%= @deal.stage.name %></p>
      </div>
    </div>
    </div>
  </div>
  <div class="hidden sm:flex">
    <%= link_to(
      'Perdido',
      account_deal_path(current_user.account, @deal, deal: { status: 'lost'} ),
      method: :patch,
      data: { confirm: 'Are you sure?' },
      class: 'rounded-md px-4 py-2 bg-auxiliary-palette-red-down text-auxiliary-palette-red text-center hover:bg-auxiliary-palette-red hover:text-white hover:no-underline font-semibold mr-2') %>

    <%= link_to(
      'Ganho',
      account_deal_path(current_user.account, @deal, deal: { status: 'won'}),
      method: :patch,
      data: { confirm: 'Are you sure?' },
      class: 'rounded-md px-4 py-2 bg-auxiliary-palette-green-down text-auxiliary-palette-green text-center hover:bg-auxiliary-palette-green  hover:no-underline font-semibold hover:text-white') %>
    
  </div>
</nav>