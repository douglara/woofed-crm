<%= stylesheet_link_tag "sidebar", media: 'all' %>

<div class="main-sidebar">
  <aside id="sidebar-wrapper">
    <div class="sidebar-brand">
      <%= link_to(root_path()) do %>
        <%= image_tag "logo.png", class: 'logo-img' %>
      <% end %>
      <a href="#" data-toggle="sidebar" class="nav-button"><i class="fas fa-bars"></i></a>
    </div>
    <div class="sidebar-brand sidebar-brand-sm">
      <%= link_to(root_path()) do %>
        CRM
      <% end %>
    </div>
    <div class="sidebar-component">

      <ul class="sidebar-menu">
        <li class="<%= 'active' if request.path.include?('pipelines') or request.path.include?('deal') %>">
          <%= link_to(account_pipelines_path(current_user.account), class: 'nav-link') do %>
          <img class="icon-menu" src="/assets/icon-deals.svg" alt="icons-deals"/><span>Negócios</span>
          <% end %>
        </li>

        <li class="<%= 'active' if request.path.include?('contact') %>">
          <%= link_to(account_contacts_path(current_user.account), class: 'nav-link') do %>
            <img class="icon-menu" src="/assets/icon-contact.svg"> <span>Contatos</span>
          <% end %>
        </li>

      </ul>
      <div class="dropdown-divider">
      </div>

      <div class="button-company">
        <ul class="sidebar-menu">
          <li class="<%= 'active' if request.path.include?('company') %>">
            <%= link_to(account_contacts_path(current_user.account), class: 'nav-link') do %>
              <img class="icon-menu" src="/assets/icon-settings.svg"> <span>Empresa</span>
            <% end %>
          </li>
        </ul>
      </div>
      
    </div>
  </aside>
</div>