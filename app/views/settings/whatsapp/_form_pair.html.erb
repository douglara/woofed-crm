<%= form_with(model: whatsapp, url: settings_whatsapp_index_path, method: :post, data: {turbo: false}, id: 'pair-form') do |form| %>
  <%= form.hidden_field :name, class: 'form-control' %>
  <%= form.hidden_field :secretkey, class: 'form-control', 'data-target': 'whatsapp-connect.secretkey' %>
  <%= form.hidden_field :endpoint_url, class: 'form-control', 'data-target': 'whatsapp-connect.endpoint_url' %>
  <%= form.hidden_field :session, class: 'form-control', 'data-target': 'whatsapp-connect.session' %>
  <%= form.hidden_field :token, class: 'form-control', 'data-target': 'whatsapp-connect.token' %>

              <%
=begin%>
 <%= form_with(model: @whatsapp, url: settings_whatsapp_new_connection_path, method: :post, data: {turbo: false}) do |form| %>
              <%= form.submit 'New QR Code', class: 'btn btn-primary mt-4' %>
            <% end %> 
<%
=end%>
<% end %>
