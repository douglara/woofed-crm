<% if action_name == 'edit' %>
  <%= link_to t('views.components.events.form.cancel_button.cancel'), account_contact_event_path(current_user.account, event.deal.contact, event), class: 'btn-secondary cursor-pointer'%>
<% else %>
  <% if action_name.include?('bulk_action') %>
    <%= link_to t('views.components.events.form.cancel_button.cancel'), new_bulk_action_account_pipeline_path(current_user.account, pipeline, stage_id: stage.id) , class: 'btn-secondary cursor-pointer' %>
  <% else %>
    <%= link_to t('views.components.events.form.cancel_button.cancel'), new_account_contact_event_path(current_user.account, event.contact, deal_id: event.deal.id) , class: 'btn-secondary cursor-pointer' %>
  <% end %>
<% end %>
