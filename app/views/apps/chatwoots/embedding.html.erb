User <%= current_user.inspect %>
Request <%= request.request_id %>

<script>
  window.addEventListener("message", function (event) {
    var event_json = JSON.parse(event.data);
    var contact = event_json.data.contact;
    const url_params = new URLSearchParams(contact).toString();
    window.location.href = "<%= chatwoot_embed_account_contacts_path(current_user.account) %>?" + url_params;
  });
</script>
