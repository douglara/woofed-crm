<%= form_for([current_user.account, company], html:{class: "flex flex-col gap-5 relative z-0 "}) do | form | %>
  <% if company.errors.any? %>
    <div>
      <ul>
        <% company.errors.each do |error| %>
          <li class="typography-body-s-lh150 text-auxiliary-palette-red"><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= render 'components/show_model/form_fields', model: company, form: form %>
  <div class="flex gap-2 self-end">
    <% if company.persisted? %>
      <%= link_to polymorphic_path([current_user.account, company]), class: "button-default-fill-secondary-sm" do %>
        <%= t('activerecord.models.cancel') %>
      <% end %>
    <% end %>
    <%= submit_button(form) %>
  </div>
<% end %>
