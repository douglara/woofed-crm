<%= form_with(model: whatsapp, url: pair_qr_code_settings_whatsapp_index_path, method: :post, data: {turbo: false}, id: 'formId') do |form| %>
  <% if whatsapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(whatsapp.errors.count, "error") %> prohibited this activity kind from being saved:</h2>

      <ul>
        <% whatsapp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :secretkey %>
    <%= form.text_field :secretkey, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :endpoint_url %>
    <%= form.text_field :endpoint_url, class: 'form-control' %>
  </div>

  <div class="card-footer text-right">
    <%= form.submit 'New connection', class: 'btn btn-primary' %>
  </div>
<% end %>
