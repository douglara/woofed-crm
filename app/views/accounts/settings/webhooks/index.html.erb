<section class="m-8 bg-light-palette-p5 border-2 border-light-palette-p3 rounded-md ">
  <%= turbo_stream_from "webhooks_#{current_user.account.id}" %>
  <ul class="px-4 py-5 grid" id="webhooks">
    <%= render partial: 'accounts/settings/webhooks/webhook', collection: @webhooks, as: :webhook %>    
  </ul>
  <div class="float-right py-6">
    <%== pagy_nav(@pagy) %>
  </div> 
</section>
