<%= turbo_frame_tag "events_to_do_#{deal.contact.id}" do %>
  <% deal.contact.events.to_do.each do |event| %>
    <%= render partial: 'accounts/contacts/events/event', locals: {event: event} %>
  <% end %>
<% end %>
