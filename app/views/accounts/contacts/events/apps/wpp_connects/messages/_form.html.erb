
<%= turbo_frame_tag "new-event-form-content" do %>

  teste form whats
  <%= form_with(model: event, url: account_contact_events_apps_wpp_connects_messages_path(current_user.account ,contact_id: @contact.id)) do |form| %>
    <% if event.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(event.errors.count, "error") %> prohibited this note from being saved:</h2>

        <ul>
          <% event.errors.each do |error| %>
            <li><%= event.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <label>Número</label>

      <select class="form-control">
        <% @wpp_connects.each do | wpp_connect | %>
          <option><%= wpp_connect.name %></option>
        <% end %>      
      </select>
    </div> 


    <div class="form-group">
      <%= form.text_area :content %>
    </div>

    <div class="card-footer text-right">
      <%= form.submit 'Salvar', class: 'btn btn-primary' %>
    </div>
  <% end %>

<% end %>