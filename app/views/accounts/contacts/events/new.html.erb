<%= render partial: '/components/events/form', locals: {
  event: @event,
  header_evolution_api_message_url: new_account_contact_event_path(@event.account, @event.contact, kind: 'evolution_api_message', pipeline: @event.deal.pipeline, deal_id: @event.deal.id),
  header_activity_url: new_account_contact_event_path(@event.account, @event.contact, kind: 'activity', pipeline: @event.deal.pipeline, deal_id: @event.deal.id),
  header_note_url: new_account_contact_event_path(@event.account, @event.contact, kind: 'note', pipeline: @event.deal.pipeline, deal_id: @event.deal.id),
  header_chatwoot_message_url: new_account_contact_event_path(@event.account, @event.contact, kind: 'chatwoot_message', pipeline: @event.deal.pipeline, deal_id: @event.deal.id)
} %>
