<section class="max-w-4xl p-8 mx-auto flex flex-col gap-8">
  <%= render 'show', contact: @contact %>
  <%= render 'deals', contact: @contact %>
</section>
<script>
  sessionStorage.setItem('chatwoot_contact_id', '<%= @contact.id %>');
</script>
