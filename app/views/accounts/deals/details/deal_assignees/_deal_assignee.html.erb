<%= turbo_frame_tag deal_assignee, class: 'grid gap-5 border-b-2 border-light-palette-p3 last:border-none py-5 first:pt-0 last:pb-0' do %>
  <%= content_tag :div, id: dom_id(deal_assignee.user), class: "grid gap-5 border-b-2 border-light-palette-p3 last:border-none py-5 first:pt-0 last:pb-0", data: {controller: 'load-lucid-icons'} do %>
    <li class= "flex items-center gap-4">
      <div class="p-2.5 rounded-lg border-2 border-light-palette-p3 bg-light-palette-p4">
        <i data-lucide="user-cog" class="w-5 stroke-dark-gray-palette-p3"></i>
      </div>
      <div class="mr-auto">
        <div class="flex items-center gap-1.5">
          <h2 class="typography-text-s-lh150 text-dark-gray-palette-p1">
            <%= deal_assignee.user.full_name %>
          </h2>
        </div>
        <%= mail_to deal_assignee.user.email do %>
          <p class="typography-text-s-lh150 text-dark-gray-palette-p4 flex items-center gap-0.5 hover:text-dark-gray-palette-p2">
            <%= deal_assignee.user.email %> <i data-lucide="arrow-up-right-square" class="w-3.5"></i>
          </p>
        <% end %>
      </div>
      <%= link_to account_deal_assignee_path(Current.account, deal_assignee), method: :delete, data: { confirm: 'Are you sure?' }, class: 'button-default-blank-secondary-icon-only-sm' do %>
        <i data-lucide="trash-2"></i>
      <% end %>
    </li>
  <% end %>
<% end %>
